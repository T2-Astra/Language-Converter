services:
  - type: web
    name: code-converter
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn converter:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
