services:
  - type: web
    name: code-converter
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn converter:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
